//Chef and Instant Noodles

#include <stdio.h>
int main() {
          int a, b;
          scanf("%d %d", &a, &b);  // Read input values for a and b
          int product = a * b;      // Print the product
          printf("%d\n", product);   // Calculate the product

return 0;
}
