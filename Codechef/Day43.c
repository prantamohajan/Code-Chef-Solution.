#Loop Output Sequence - MCQ

#include <stdio.h>
int main() {
          int y = 1;
          while (y >= 1 && y <= 3) {
                    printf("%d ", y);
                    y++;
          }
          return 0;
}